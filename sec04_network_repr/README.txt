§4 — Multilayer network representation

A_l = 1[M_l > 0], directed 60×60 binary per layer.
Layer constants c_l: IG,IC=5; RAC,IAC_RAC,IAC_RDE,IRAC=3; MM/MC phenotypes=2; CCAM=1.
c_l enters only Σ_l c_l·M_l (visualization); cancels in all per-layer analyses.

Categories:
  DA = IG, IC, RAC
  IA = IAC_RAC, IAC_RDE, IRAC
  MM = RP, IRP
  MC = CCAM, CCVM, CMP, CS

Script: data_io.py
Output: none (loader).

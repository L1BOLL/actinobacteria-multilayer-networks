§13 — Spatial dependence

Qualitative only: contact denser for IG/IC; sporulation/pigmentation comparable
across configurations. v14 "0.08 → 0.22, p<0.001" not reproducible — dropped.

Planned (pending per-configuration data):
  per-(layer, configuration) density, reciprocity, SCC
  per-layer spatial-sensitivity index across 12 phenotypes

Script: p2_9_spatial_decomposition.py
Behaviour: auto-skips if per-configuration matrices absent.
Required input: contact / proximity / distant adjacency per layer.

Outputs: none currently. Will populate phase2_outputs/tables/p2_9_*.csv + figure.
